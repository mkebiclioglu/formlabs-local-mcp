/**
 * Placeholder. The Vercel bootstrap (deploy/bootstrap.sh) overwrites this file
 * with the deployment's configuration because this project's env vars are not
 * managed through the Vercel dashboard. Locally, .env.local is used instead.
 */
export const GENERATED_ENV: Record<string, string> = {};
